---
title: Cartolang Test for Contentlayer
date: '2025-07-19'
tags: ['cartolang', 'langdeck', 'contentlayer']
draft: false
summary: Principes d'utilisation de Cartolang 
---

# Langdeck Documentation

Ce document explique les opérations importantes nécessaires pour installer et déployer les différentes applications du projet Langdeck.
 
## Installation
### Installation Node.js et npm
Au démarrage de chaque nouveau projet React, ou à son installation sur un nouveau serveur, il est nécessaire d'installer les packages suivants.

## Code Blocks
```bash
sudo apt update
sudo apt upgrade
sudo apt install nodejs npm -y
```


Some Javascript code

```javascript
  return (
    <div className="w-full h-[200px] md:h-[400px] bg-blue-50 dark:bg-gray-800">
      <div ref={chartRef} className="w-full h-full" />
      {selectedCountry && (
        <>
          <p className="font-articulat_cf text-center mt-2 text-xl font-base text-blue-500">
            Pays sélectionné : {selectedCountryNameFr} <CheckCircleOutlineIcon /> (OK pour valider) 
          </p>
        </>
      )}
      {selectedContinent && (
        !selectedCountry ? (
          <p className="font-articulat_cf font-base text-center mt-2 text-xl">
            Continent sélectionné : {continentSettings[selectedContinent].name} (cliquez de nouveau pour choisir)
          </p>
        ) : (
          <></>
        )
      )}
    </div>
  );
```

Some Python code 🐍

```python
def replace_substring (s):
  ssub1 = s.split(" ")[-2]
  ssub2 = "___"
  s2 = s.replace(ssub1, ssub2)
  return s2
```
